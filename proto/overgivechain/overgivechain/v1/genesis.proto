syntax = "proto3";
package overgivechain.overgivechain.v1;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "overgivechain/overgivechain/v1/params.proto";

option go_package = "overgive-chain/x/overgivechain/types";

// GenesisState defines the overgivechain module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
}
